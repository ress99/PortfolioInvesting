git clone é a primeira coisa
git pull traz os commits que não tinha e tenta aplicar as diferenças - traz as novidades
git push mete no servidor
git commit é sempre local
git status diz o que mudou
git add seleciona os ficheiros para fazer commit
Não deixar coisas penduradas no add
add e commit logover git diff
git commit -m 'descrição'
git checkout volta para o último checkout

